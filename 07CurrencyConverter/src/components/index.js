// so when ever we create different components for different things we endd to import it in app.js which is very is tedious to import all component one by one
// so we will create index.js and import all the components here
import InputBox from "./InputBox";

export { InputBox };
